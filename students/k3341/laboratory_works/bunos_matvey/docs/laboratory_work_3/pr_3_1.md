# Отчет ПР3.1, Бунос Матвей

## Задание 1

### Описание

Добавить 6 новых автовладельцев и 5 автомобилей, назначив каждому владельцу от 1 до 3 машин.

### Код

```python
from project_first_app.models import CarOwner, Car, Ownership
from datetime import datetime

owner1 = CarOwner.objects.create(first_name="Sergey", last_name="Ivanov", birth_date=datetime(1985, 5, 14))
owner2 = CarOwner.objects.create(first_name="Olga", last_name="Smirnova", birth_date=datetime(1992, 9, 30))
owner3 = CarOwner.objects.create(first_name="Dmitry", last_name="Kuznetsov", birth_date=datetime(1988, 2, 19))
owner4 = CarOwner.objects.create(first_name="Anna", last_name="Popova", birth_date=datetime(1975, 11, 23))
owner5 = CarOwner.objects.create(first_name="Mikhail", last_name="Sokolov", birth_date=datetime(1991, 7, 8))
owner6 = CarOwner.objects.create(first_name="Ekaterina", last_name="Lebedeva", birth_date=datetime(1995, 4, 16))

car1 = Car.objects.create(brand="Ford", model="Focus", color="White", license_plate="XYZ123")
car2 = Car.objects.create(brand="BMW", model="320i", color="Black", license_plate="XYZ234")
car3 = Car.objects.create(brand="Mercedes", model="C-Class", color="Silver", license_plate="XYZ345")
car4 = Car.objects.create(brand="Volkswagen", model="Golf", color="Blue", license_plate="XYZ456")
car5 = Car.objects.create(brand="Audi", model="A4", color="Red", license_plate="XYZ567")

Ownership.objects.create(owner=owner1, car=car1, start_date=datetime(2024, 1, 10))
Ownership.objects.create(owner=owner2, car=car2, start_date=datetime(2024, 2, 15))
Ownership.objects.create(owner=owner3, car=car3, start_date=datetime(2024, 3, 22))
Ownership.objects.create(owner=owner4, car=car4, start_date=datetime(2024, 4, 18))
Ownership.objects.create(owner=owner5, car=car5, start_date=datetime(2024, 5, 30))
Ownership.objects.create(owner=owner6, car=car1, start_date=datetime(2024, 6, 12))
Ownership.objects.create(owner=owner6, car=car3, start_date=datetime(2024, 7, 25))
Ownership.objects.create(owner=owner1, car=car5, start_date=datetime(2024, 8, 14))
```

### Результат

**Владельцы:**
Sergey Ivanov, Olga Smirnova, Dmitry Kuznetsov, Anna Popova, Mikhail Sokolov, Ekaterina Lebedeva

**Машины:**
Ford Focus, BMW 320i, Mercedes C-Class, Volkswagen Golf, Audi A4

## Задание 2

### Описание

Выполнить фильтрацию данных.

1. **Все машины марки BMW:**

```python
bmw_cars = Car.objects.filter(brand="BMW")
for car in bmw_cars:
    print(car)
```

**Результат:**
```
BMW 320i (XYZ234)
```

2. **Владельцы, чьи имена содержат "Anna":**

```python
anna_owners = CarOwner.objects.filter(first_name__icontains="Anna")
for owner in anna_owners:
    print(owner)
```

**Результат:**
```
Anna Popova
```

3. **Случайный автомобиль и его владелец:**

```python
import random
random_car = random.choice(list(Car.objects.all()))
owner = random_car.ownership_set.first().owner
print(f"{random_car} - {owner}")
```

**Результат:**
```
Mercedes C-Class - Dmitry Kuznetsov
```

4. **Все владельцы машин красного цвета:**

```python
red_cars = Car.objects.filter(color="Red")
red_car_owners = CarOwner.objects.filter(ownership__car__in=red_cars).distinct()
for owner in red_car_owners:
    print(owner)
```

**Результат:**
```
Mikhail Sokolov
Sergey Ivanov
```

5. **Владельцы машин, начавшие владение с 2024 года:**

```python
owners_from_2024 = CarOwner.objects.filter(ownership__start_date__year=2024).distinct()
for owner in owners_from_2024:
    print(owner)
```

**Результат:**
```
Sergey Ivanov
Olga Smirnova
Dmitry Kuznetsov
Anna Popova
Mikhail Sokolov
Ekaterina Lebedeva
```

## Задание 3

### Описание

Использовать аннотации и сортировки для анализа данных.

1. **Самая ранняя дата начала владения машиной:**

```python
print(Ownership.objects.earliest('start_date').start_date)
```

**Результат:**
```
2024-01-10
```

2. **Количество машин у каждого владельца:**

```python
from django.db.models import Count

owners_with_car_count = CarOwner.objects.annotate(car_count=Count('ownership__car'))
for owner in owners_with_car_count:
    print(f"{owner.first_name} {owner.last_name} - {owner.car_count}")
```

**Результат:**
```
Sergey Ivanov - 2
Olga Smirnova - 1
Dmitry Kuznetsov - 1
Anna Popova - 1
Mikhail Sokolov - 1
Ekaterina Lebedeva - 2
```

3. **Количество машин каждой марки:**

```python
car_brand_counts = Car.objects.values('brand').annotate(count=Count('brand'))
for brand_count in car_brand_counts:
    print(f"{brand_count['brand']}: {brand_count['count']}")
```

**Результат:**
```
Ford: 1
BMW: 1
Mercedes: 1
Volkswagen: 1
Audi: 1
```

4. **Сортировка владельцев по дате выдачи удостоверения:**

```python
sorted_owners_by_license_date = CarOwner.objects.filter(driverlicense__isnull=False).order_by('driverlicense__issue_date')
for owner in sorted_owners_by_license_date:
    license_date = owner.driverlicense_set.first().issue_date
    print(f"{owner.first_name} {owner.last_name} - {license_date}")
```

**Результат:**
```
Sergey Ivanov - 2024-01-10
Olga Smirnova - 2024-02-15
Dmitry Kuznetsov - 2024-03-22
Anna Popova - 2024-04-18
Mikhail Sokolov - 2024-05-30
Ekaterina Lebedeva - 2024-06-12
```